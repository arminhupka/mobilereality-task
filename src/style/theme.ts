export interface ThemeInterface {
	primaryColor: string
	primaryColorDark: string
	gradient: string
}

const theme: ThemeInterface = {
	primaryColor: '#009eff',
	primaryColorDark: '#008ee6',
	gradient: 'radial-gradient(#009eff, #006ead)',
}

export default theme
